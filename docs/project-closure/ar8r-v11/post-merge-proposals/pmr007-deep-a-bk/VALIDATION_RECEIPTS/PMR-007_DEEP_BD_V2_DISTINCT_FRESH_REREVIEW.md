# PMR-007 Deep BD V2 distinct fresh rereview

```text
candidate: PMR-007-LSRO-1 V2
frozen packet: PMR-007_DEEP_BD_V2_FROZEN_HASHES.sha256
rereview method: independent tensor contraction with exact random rational confusion matrices
review relation: distinct implementation, same model lineage
external human or model-lineage independence: NOT CLAIMED
result: PASS_WITH_NONBLOCKING_SOURCE_AND_IDENTIFIABILITY_NOTES
```

## Frozen-byte verification

```text
frozen hash rows checked: 6
hash mismatches: 0
```

## Independent semantic checks

The rereview did not call the primary enumeration functions.  It constructed
latent-component tensors by repeated Kronecker products, mixed them by exact
rational priors, swapped the latent rows, and compared the complete observable
tensors.

```text
random exact-rational models: 50,000
sources per model: 3 through 6
latent-swap tensor failures: 0
posterior-complement failures: 0
observable-signature failures: 0
strict directional-anchor cases: 45,859
strict directional-anchor failures: 0
```

The independent copy-map control used one root of accuracy `4/5`, equal latent
prior, and the deterministic map `Y -> (Y,Y,Y,Y)`:

```text
root likelihood ratio on report 1: 4
four deterministic copies likelihood ratio: 4
incorrect independent fourth power: 256
control: PASS
```

## Scope rereview

The V2 packet now correctly separates:

```text
observational latent-class identification;
orientation of arbitrary latent labels;
report-symbol semantics;
world-truth reference;
source calibration;
root authentication;
version applicability;
and creed-internal warrant.
```

The calibrated-root theorem is conditional on component recovery up to exactly
one swap orbit.  No generic latent-class identifiability result is claimed.
Neither source agreement nor route independence establishes the world
orientation.  A source-relative premise can restrict a Track-N model class, but
that restriction is not silently migrated into neutral evidence.

## Nonblocking notes

1. The theorem is a direct application of latent-class label-switching theory;
   its value is the exact source–world authority firewall and cross-lane
   placement, not general mathematical novelty.
2. The finite conditional-independence model is not a reconstruction of
   historical tawatur theory.
3. Calibration evidence remains vulnerable to semantic drift, domain shift,
   stale versions, source deception, and actual-world/model-class mismatch.
4. External statistical and source-specialist review remain open.

## Disposition

```text
PASS_WITH_NONBLOCKING_SOURCE_AND_IDENTIFIABILITY_NOTES
```

The rereview supports admission only at the scoped finite statistical and
source-custody level stated in V2.
