# PMR-007 Deep Round A V1 — cold audit

```text
audit epoch: B
candidate identity: PMR-007-NFG-1
frozen packet: PMR-007_DEEP_A_V1_FROZEN_HASHES.sha256
disposition: PASS_WITH_NONBLOCKING_SCOPE_AND_AUTHORITY_NOTES
blocking findings: 0
```

## Hash and custody audit

The five frozen owners named in the hash receipt were recomputed before review.
No mismatch was found.  The source note records packet-relative evidence labels
and SHA-256 values rather than importing private source bytes into the theorem.

## Mathematical audit

`NFG-1` is correct in its declared finite deterministic setting.  It is the
standard factor-through-a-quotient/fibre criterion: a Boolean target factors
through a profile exactly when it is constant on every profile fibre.  The two
proof directions are complete, including the off-image extension.

The horn discrimination does not depend on the theorem alone.  Each stronger
inference is separately tested by a model in which the function-fixing relation
survives while the target, truth-link, proper operation, teleology, or Wisdom
coordinate is withheld.

## Nonblocking findings

### DA-N01 — ancestry and novelty ceiling

The abstract theorem is a direct instance of the established profile/fibre
family.  It receives no independent general theorem or novelty credit.  Its
value is the proper-function application and the explicit Candidate-E horn
adjudication.

### DA-N02 — independent truth target is load-bearing

`T` is independently fixed.  If a horn is permitted to define `T`, the theorem
becomes vacuous as a truth-link test.  The packet correctly states this, but all
later uses must retain the independence guard.

### DA-N03 — L2 does not entail L3

A profile may fix the truth-linked target while the mechanism remains
unreliable, improperly functioning, defeated, provenance-invalid, or used in
an unsuitable environment.  The packet correctly preserves this separation.

### DA-N04 — proper function does not entail teleology or Wisdom

Selected-effect, organizational, constitutive-truth, primitive-norm, and
source-relative models can each satisfy some function coordinate without a
personal designer or wise end.  The packet correctly withholds the Wisdom
bridge.

### DA-N05 — fiṭrah authority class

The fiṭrah model is school-internal/source-relative unless source truth,
translation, applicability, and a neutral bridge are independently supplied.
It cannot be migrated into a neutral transcendental entailment.

### DA-N06 — executable scope

The checker verifies the finite factorization criterion and registers the
model controls.  It is not empirical evidence for biological, cognitive,
teleological, or theological proper function.

## Rereview requirements

A distinct rereview must:

1. verify the frozen hashes;
2. decide factorability by brute enumeration of candidate profile functions,
   not by reusing the primary checker implementation;
3. test randomly generated profile maps and targets across several finite
   cardinalities;
4. preserve the L0/L1/L2/L3 and source-neutral/source-relative firewalls.
