# PMR-007 Deep Round BG V1 — integrated R5 one-bearer impersonal common model

```text
identity: PMR-007-IR5CM-1
round: DEEP_BG
version: V1
status: POST_MERGE_RESEARCH_CANDIDATE
historical_identity: NONE
repository_mutation: NONE
central_burden:
  whether the currently admitted neutral structure forces a unified personal,
  intellectual, wise, or speaking ground
```

## 1. Declared neutral theory

`T_R5+` is a finite many-sorted theory with worlds, one structural bearer,
contents, actions, effects, expression tokens, and a latent interaction field.
It requires one bearer `g` to satisfy all of the following in every declared
world:

```text
N1  model-relative necessary presence;
N2  underived status in the declared dependency graph;
N3  external actuality and causal efficacy over declared effects;
N4  one-bearer co-instantiation of modal-order, semantic-registration,
    norm, selection, efficacy, and expression roles;
N5  a truth-labelled content coordinate;
N6  a non-self-authorized target key;
N7  an exact additive norm field;
N8  action selection guided by the encoded norm;
N9  the selected action is objectively fitting in the declared evaluation;
N10 faithful token production for the registered content;
N11 a finite joint interaction with an impersonal latent-product realization;
N12 one source-functional-role bearer under a declared candidate interpretation.
```

The theory intentionally contains no axiom identifying those structural roles
with intellect, first-person ownership, personality, intentional uptake,
selection because of fittingness, Wisdom, divine Speech, or Creatorhood.

## 2. Candidate result

There is a finite model of `T_R5+` with one common structural bearer and with
all of the following false of that bearer:

```text
Intellect;
Personal;
Agentic;
IntentionalUptake;
BecauseFitting;
Wisdom;
SpeechCapacity;
ActualSpeech;
CreatorClass.
```

Moreover, the same complete neutral reduct has expansions assigning these nine
predicates arbitrarily. Hence no predicate in that list factors through the
complete declared neutral reduct.

## 3. Strongest impersonal witness

The witness has two declared worlds, one bearer `g`, two contents, two actions,
two effects, and two expression tokens.

```text
- g exists in both declared worlds;
- g is underived in the declared dependency graph;
- g actualizes effects and is therefore not merely an abstract node;
- the true content is registered by a primitive semantic anchor;
- the norm target equals that truth coordinate;
- the selected action tracks the norm and produces the declared fitting effect;
- the output token faithfully expresses the registered content;
- the joint X/Y law is diag(1/2,1/2) and has an impersonal width-2 realization;
- all structural roles are assigned to g;
- every personal, intentional, Wisdom, Speech, and CreatorClass predicate is false.
```

This witness grants one bearer and aligned structural roles. Its point is not
that those primitive anchors are explained, but that even granting them does
not deductively supply the later personal predicates.

## 4. Personal twin and deletion controls

A personal expansion sets all nine conclusion predicates true while leaving
every neutral relation and function unchanged. For each conclusion predicate,
a deletion expansion sets only that predicate false while keeping the other
eight true. All such expansions satisfy `T_R5+`.

Thus the following remain independently load-bearing:

```text
H7  content determinacy requires intellect;
H8  intellect entails agency/personality;
H11 knowledge/selection/efficacy are intentionally and normatively integrated;
H15 capacity-to-occurrence and source/naming/content bridges;
and a separate actualizer-to-Creator bridge.
```

## 5. Candidate-G carrier-boxing control

A family of same-bearer models permits the semantic anchor, norm target,
selector, efficacy map, and expression map to recombine unless cross-role laws
are added. Shared carrier identity therefore does not by itself produce
shared derivation, primitive reduction, or explanatory compression.

The aligned witness adds cross-role equalities, but those equalities can be
implemented as an impersonal law. Alignment is a genuine restriction of the
product profile space; it is not yet a personal unifier.

## 6. Track and source firewalls

The source-role coordinate is only a functional-role map under one declared
candidate interpretation. It does not authenticate the source, select the
actual world, verify Arabic wording, migrate Track-N personality/Speech
predication into Track T, or establish the world truth of the neutral theory.

## 7. Proposed central disposition

```text
B6 one common bearer:
granted in the strongest witness

B7 content -> intellect:
not entailed

B8 intellect -> personality/agency:
not entailed

B11 structural knowledge-selection-efficacy -> Wisdom:
not entailed

B15 expression capacity -> actual divine Speech:
not entailed

actualizer -> CreatorClass:
not entailed

R5:
strengthened by a one-bearer impersonal common model
```

## 8. Initial authority ceiling

This is a finite signature-relative nonentailment candidate. It does not prove
metaphysical possibility, equal probability, physical realizability, source
truth, or the falsity of a personal ground. General mathematical novelty is
zero.
