# Specialist A formal/computational candidate — sanitised review proposal

```text
repository authority read: 9b80f2dfdf73a768ccad6a6ea2f2998c70ebdcf3
proposal status: independent review only
GitHub mutation: none
repository adoption: none
owner adoption: not claimed
novelty: 0
historical identity: none
Lean machine claim: none
```

This packet packages twelve specialist-local formal/computational operations
for independent repository review. It is not a controlling checkpoint and does
not allocate authoritative theorem or proposal IDs.

## Main contribution

The packet supplies a typed eight-factor finite registry, an exact 22-family
declared coverage map, a common-decoder criterion, deletion countermodels,
resource/Pareto normalisation, partial-observation and common-knowledge guards,
provenance-root accounting, representation-fibre metrics, residual-collision
gain and exact minimum cover, a finite adaptive history-state twin, and a
predictive-history quotient.

The combined decision chain is:

```text
eligibility and typing
  -> common target-blind decoder condition
  -> residual cross-target profile collisions
  -> eligible factor coverage
  -> exact minimum cover or declared-family no-cover
  -> conditional history-state class-closure nonidentifiability
  -> Pareto and predictive-quotient resource correction
```

No actual unified-personal, impersonal-powers, or plural architecture is
implemented here. The finite architecture names in the registry are explicitly
constructed signature witnesses.

## Reproduction

From this directory, with Python and the pinned review dependencies:

```bash
python checks/run_all_checks.py
python audit/fresh_rereview.py
```

Expected bounded result:

```text
12/12 executable round checkers PASS
79/79 declared mutants killed
13/13 procedurally distinct rereview checks PASS
```

The rereview reimplements the load-bearing finite claims without importing the
generation checker modules. It is still local procedural independence, not an
external reviewer.

## Lean status

`lean/DeletionCriterion.lean` and `lean/ObservationUniformity.lean` are
source-only drafts. Parse, elaboration, kernel checking, project build, and
axiom reporting were not run. The exact machine claim is `NONE`.

No duplicate theorem is proposed for the common-decoder result: it is the
existing finite T294/Deep-BV fibre factorisation criterion on a tagged disjoint
union. The partial-observation result remains a specialisation of the existing
finite co-Büchi family. Set cover, history-state realisation, and predictive
quotient results receive no novelty by default.

## Review ceilings

The packet does not establish:

- actual architecture implementation or class membership;
- measurable, stochastic, infinite-horizon, or adversarial extensions;
- a resource lower bound over a complete rival class;
- proper function, warrant, personality, source truth, or source-world transfer;
- metaphysical identification, meniscus, champion, admission, or closure.

The next admissible operation requires an externally reviewed actual
implementation map, a common semantic crosswalk, and an independently eligible
target-blind candidate factor—or a separately justified architecture-class
closure/resource premise.

Read `CENTRAL_INTEGRATOR_RECEIPT.yaml`, `BURDEN_LEDGER.yaml`, and the six
`crosswalk/` receipts before considering any subset for repository admission.
