# R6 Review E — reproducibility / build

Surfaced model: `claude-fable-5`. A brand-new virtual environment was created and populated ONLY from `requirements-ci.lock.txt`; the complete 34-step suite ran green under it with a clean tree (including the deterministic PDF byte-equality rebuild). State convergence green; packet freeze hashes recompute (FCSP-1 re-frozen after the Review-B repair); manifest regenerated last at every commit; no network access in any validator (offline by construction; CI installs from the lock as its only fetch). The converter change (dropping pure HTML comments) was reviewed against the strict no-silent-skip contract: only comment tokens — never content — are skipped, and the text-structure QA still passes on all four PDFs, all pages re-rendered and inspected (References pages verified visually). Reproducibility is claimed under the recorded toolchain only. No blocking findings.
