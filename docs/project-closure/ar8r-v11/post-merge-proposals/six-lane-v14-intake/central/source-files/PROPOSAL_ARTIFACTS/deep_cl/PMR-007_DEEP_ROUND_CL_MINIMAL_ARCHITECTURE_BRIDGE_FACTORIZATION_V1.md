# Deep CL V1 — minimal architecture-bridge factorization

## Candidate identity

```text
identity: PMR-007-MABF-1
status: POST_MERGE_RESEARCH_CANDIDATE
historical identity: NONE
```

Let the declared architecture class be `Q ∈ {U,I,P}` over the concrete Deep-CK model class. For an anchor `a`, write `a(m)` for its value on architecture model `m`. A set of anchors identifies `Q` exactly when the joint anchor profile is constant only within one `Q`-class.

Equivalently, for every architecture-conflict pair `m,m'` with `Q(m) != Q(m')`, at least one selected anchor has `a(m) != a(m')`. Thus architecture identification over a frozen finite registry is a conflict-cover problem.

In the Deep-CK registry:

```text
complete neutral experiment: {U,I,P}
common bearer:              {U,I} | {P}
intentional uptake:         {U} | {I,P}
```

The meet of the common-bearer and intentional-uptake partitions is discrete. Neither partition alone identifies the three architecture classes. Therefore the architecture burden factorizes into two independent bridge demands:

```text
B_common:
separate plural realization P from the one-bearer classes U and I;

B_uptake:
separate unified personal U from one-bearer impersonal I.
```

The certified neutral profile covers neither residual. A Track-N intensional-predication anchor has the same U-versus-I/P partition as intentional uptake only conditional on the complete source/world/Wisdom guard package.

This candidate calls the two-anchor pair minimal and treats its cardinality as the residual bridge burden for the three-architecture model class.
