# Fable research steer: OSM learning trajectories and orthemological convergence

Work in the clean public `orthemology` repository checkout supplied by the
owner. Do not work in, scan, or commit from any private recovery workspace.

## Authority and starting state

Start from the exact checked-out commit and record it before editing. Read the
repository instructions, status owners, Decision 0015, Decision 0024, `LAT-1`,
the V12 OSM crosswalk, the latent-state application, the AR8R V11 milestone projection,
the research-flywheel crosswalk, the proper-function and ontology matrices, the
transcendental bridge/rival ledger, the formalization queue, and the repository
privacy rules.

The focal primary source is:

```text
Sun et al.
Learning produces an orthogonalized state machine in the hippocampus
DOI 10.1038/s41586-024-08548-w
exact local access-copy SHA-256:
0d097cba7bbb25a949e2bf95af28b5a2259bd8d60b0e5fac5a74cdf7d05aa814
```

The article is already integrated as source `LAT-1` and through Decisions 0015
and 0024. Do not redo source ingestion or claim that the source was missing.
Treat the local access copy as custody-only. Never commit it.

## Research objective

Independently test whether the paper's endpoint-versus-trajectory,
observation-aliasing, latent-transition, architecture/objective, and task-
alteration results contribute to a real mathematical convergence across
orthemology's lanes.

The goal is not to make the topics sound alike. A positive convergence result
must exhibit at least one of:

```text
an exact reduction;
a shared formal model with typed interpretation maps;
a shared invariant with domain-specific semantics;
a guarded transfer theorem;
or one countermodel architecture under matched assumptions.
```

Otherwise classify the relation as a guarded specialization, analogy,
boundary-only relation, or nonidentity.

## Required research packages

1. **Primary-source and code audit.** Verify the exact article claims, methods,
   limitations, data/code locators, and the reported comparison among CSCG,
   RNN, LSTM, transformer, and Hebbian-RNN settings. Separate observation,
   author interpretation, and speculation. Do not infer that comparative fit
   identifies the biological mechanism.

2. **Formal object extraction.** Type world state, observation, sequence,
   latent state, posterior, representation, geometry, update rule, objective,
   trajectory, endpoint, intervention, and alteration. State every identity and
   nonidentity relation explicitly.

3. **Endpoint-trajectory classification.** Determine whether the reported
   distinction is an exact instance, guarded specialization, or analogy of the
   repository's result-versus-pathway architecture. Construct countermodels to
   stronger readings, including same endpoint with different trajectories,
   high performance without target geometry, and trajectory fit without
   mechanism identity.

4. **Candidate mathematics.** Where evidence permits, formulate and test narrow
   candidates concerning trace quotients, trajectory-discrimination power,
   active interventions that separate observationally equivalent models, and
   guarded transport across altered cues or task geometry. Give new identifiers
   to new work. Do not represent it as historical AR8R recovery.

5. **Cross-lane comparison.** Compare any surviving formal core against proof
   audit traces, agent reason transport, version custody, TAC/SAC lineage and
   provenance, noetic diagnosis, restorative trajectories, proper-function
   evaluation, occurrence-sensitive language, and source-formal-
   implementation-world crosswalking. For each relation, give the exact map,
   guards, failure condition, and nontransfer.

6. **Transcendental firewall.** The paper supplies no evidence for human noetic
   architecture, soul carriers, proper function, objective warrant,
   transcendental orthability, Necessary Being, unity, agency, attributes,
   Speech, or revelation. If those lanes use a formal result, label it as a new
   project-side conditional application and show every bridge premise. Do not transfer neuroscience evidence into metaphysics or theology.

7. **Independent validation.** For every theorem candidate, freeze the exact
   statement, prove it independently, search for deletion and stronger-reading
   countermodels, run finite checks where meaningful, audit ancestry and prior
   art, conduct a cold audit, repair blocking findings, and conduct a distinct
   rereview. Mathematical validity alone does not establish historical
   identity, empirical truth, novelty, or meniscus.

8. **Repository contribution.** Update only public-safe owners when the result
   is materially new and fully supported. Add source rows for any new
   load-bearing literature. Keep source reports, project syntheses, conjectures,
   proofs, experiments, and metaphysical theses separate. Extend fail-closed
   validators and tests for every new authority boundary.

## Git rules

Use branch:

```text
fable/ar8r-convergence-research-v1
```

Before creating or pushing that branch:

1. require a clean worktree and record `git status --short --branch`;
2. verify that `origin` identifies `theislampill/orthemology`;
3. run `git fetch origin --prune` and record the fetched base SHA;
4. test whether `origin/fable/ar8r-convergence-research-v1` already exists;
5. if it exists, stop unless its exact head is the owner-supplied expected head
   or it is already the verified ancestor of the local continuation;
6. never overwrite, reset, delete, or force-update an unexpected remote branch.

You may create commits and push this branch when the changes contain material
research or validation work. Never push directly to `main`, force-push, merge,
or publish private evidence. Open a draft pull request only after the branch is
clean and the repository workflow passes.

Every commit body must state:

```text
base SHA and prior head;
research question;
sources and exact versions;
formal result or negative result;
countermodels and failed candidates;
claims and nonclaims;
files changed;
validation commands and exit codes;
unverified burdens;
privacy result;
and whether champion, meniscus, or closure changed.
```

No commit may self-certify a champion, meniscus, natural closure, empirical
validation, external peer review, Lean/kernel proof, or metaphysical bridge.

## Stop condition and report

Continue through dependency-ready work until a real blocker exists, no
materially distinct ready route remains, or the environment imposes an actual
execution boundary. Do not stop after merely restating the roadmap or producing
one analogy.

At the checkpoint report:

```text
exact base and head SHA;
primary sources checked;
formal correspondences established;
guarded transfers established;
analogies or nonidentities established;
candidate theorems and proofs;
countermodels and failed candidates;
cross-lane flywheel effects;
files changed;
tests and exit codes;
commit and push receipts;
draft PR, if any;
remaining burdens;
integrated champion status;
meniscus status;
natural-closure status;
exact next atomic action;
exact reason work stopped.
```
