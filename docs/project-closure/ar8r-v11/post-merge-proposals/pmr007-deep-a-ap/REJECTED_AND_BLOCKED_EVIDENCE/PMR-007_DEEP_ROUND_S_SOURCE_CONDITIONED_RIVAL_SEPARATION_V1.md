# PMR-007 Deep Round S V1 — source-conditioned rival separation

## Candidate identity

```text
PMR-007-SCRF-1
POST_MERGE_RESEARCH_CANDIDATE
```

## Question

Can the translated-primary Asfahani predicate bundle select the unified personal/source architecture over the current abstract, impersonal, plural, silent, created-speech, and deistic rivals?

## Candidate model

Let `L_T` be the neutral Track-T language and extend it by source predicates `Σ_N`. A source extension designates one bearer `g` and records predicates attributed to that same referent. For a finite rival family `R`, let `D(r) ⊆ Σ_N` be the predicates that rival `r` withholds. Define the source-separation number:

```text
sigma_N(R) = min{|S| : S intersects D(r) for every r in R}.
```

Candidate claim: a predicate set `S` eliminates the declared rival family exactly when `S` is a transversal of the defect hypergraph `{D(r)}`. The Asfahani source bundle supplies such a separator, and therefore supports the unified personal architecture over the declared rivals.

## Source loci

The accessible translation states that the cosmos has a necessarily existent Creator who is one, knowing, able, living, characterized by volition, speaking, hearing, and seeing; it separately says the meanings of volition and speech are true attributes although `al-murid` and `al-mutakallim` are not unqualified divine names; and it presents the Qur'an as uncreated speech subsisting in and spoken by Allah. The same source also warns that an argument yielding merely some necessary existence does not yet affirm the Creator.

Authority: translated-primary access only; no Arabic-primary verification.

## Strong reading

Under authenticated source truth and correct referent identification, the full source bundle excludes every rival in the frozen portfolio. No neutral-world conclusion is intended beyond the conditional source expansion.
