# PMR-007 Deep BE equation-presentation correction

The preserved snapshot file `VALIDATION_RECEIPTS/AR8R_PMR007_DEEP_BE_EQUATION_AND_SOURCE_CUSTODY_ADDENDUM_V1.md` contains a malformed TeX table rendering: several array rows end with a single backslash and place punctuation inside the array. The authoritative boxed equation later in that same preserved file remains unambiguous. This correction supersedes only the malformed presentation, not the source packet or mathematical status.

The corrected presentation is:

```math
\begin{aligned}
\Sigma_{\mathrm{source}}(x)
&= (T(x),S(x),A(x),V(x),P(x)),\\
\Sigma_{\mathrm{formal}}(x)
&= (D(x),R(x),G(x),C(x)).
\end{aligned}
```

The exact symbol meanings and authoritative equation remain those stated in the preserved source. This overlay grants no new theorem, proof, source, novelty, adoption, or meniscus credit.
