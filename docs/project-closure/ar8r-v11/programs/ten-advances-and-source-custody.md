# Ten Advances and source/formalization custody

## Historical evidence

The recovered V5 Lean inventory contains 230 declaration records across 48
scanned source files: 221 declarations with source present, 122 theorem/lemma
records, nine intended declarations without mounted source, and twelve principal
external Ten Advances declarations. The inventory was regex/static-source level.

## Current status

`SOURCE_CUSTODY_PRESENT_FORMAL_VALIDATION_NOT_PERFORMED`. There is no completed
external human review, source-specialist review, local Lean build, elaboration,
or kernel-check receipt for the aggregate inventory.

## Admitted results

- exact source presence and declaration-name custody are recorded where available;
- external declarations receive zero general AR8R theorem credit;
- source mapping may establish a candidate ancestry relation, not theorem identity;
- missing mounted source remains source-absent rather than silently reconstructed.

## Open burdens

- retrieve and hash all intended sources where authorized;
- run the matching Lean toolchain and dependency lock;
- parse, elaborate, and kernel-check each claimed declaration;
- independently review the ten source/formalization dossiers;
- adjudicate prior art and exact theorem ancestry;
- preserve source-relative assumptions and license boundaries.

## Forbidden claims

- source presence implies parsing, elaboration, or kernel checking;
- declaration names establish theorem identity;
- external repositories confer AR8R novelty or origin credit;
- static scans are formal proof verification;
- the Ten Advances packet has completed external human review.

## Repository effect

Expose the bounded custody and verification burden through the V11 Lean map.
No external theorem is adopted or credited as an AR8R origin by this charter.

The static Comparator count remains `CONFLICTING_38_42`: 42 is reported at source-card lines 116–117 and 38 at lines 1007–1009. The package also lacks `ComparatorChallenges.C_PermanentSuperquadraticStandalone` and conflicts over `MetricCodes/Base.lean` versus `MetricCodes.lean`. Local build, parsing, elaboration, kernel checking, `#print axioms`, and Comparator execution remain unperformed.

The current Connes-rigidity dispute is recorded separately: OpenAI is `NIELSEN_CHALLENGE_REJECTED__SPECIALIST_SETTLEMENT_OPEN`; the Nielsen critique `DOES_NOT_REFUTE_EXACT_PUBLIC_OPENAI_OBJECT`; the Nielsen positive proof is `REJECTED_AS_PROOF__FATAL_GAPS`; and the overall status is `UNRESOLVED_PENDING_OPERATOR_ALGEBRA_SPECIALIST_REVIEW`. Exact-source custody and zero static proof-hole tokens do not supply a build, kernel, specification-correspondence, or operator-algebra review receipt.
