# AR8R V11 deferred, superseded, and private-excluded evidence

Status: V11 repository-reconciliation control surface. This document records why recovered evidence is not silently promoted into current theory.

## Coverage receipt

The public evidence catalog contains 128 claim-level rows, supplemented by
sanitized structural closure maps:

- 65 individually rehashed V8 downloads: 65 files present, 65 unique SHA-256 values, zero size or hash mismatches;
- 61 cross-surface lane, theorem, candidate, correction, provenance, PMR, and
  custody rows derived from the complete V7 transcript, the 44 Activity
  controls, 1,127-row attachment index, 464-row first-party custody ledger,
  17,293-row Activity file-reference ledger, archive members, supplied
  correction packets, and current-main V8 authority;
- 43 Activity logs structurally assessed from stabilized flyout captures;
- one Activity control preserved with the exact `ACTIVITY_FLYOUT_UNAVAILABLE` boundary.

The V13 extension also binds the later authenticated-thread source universe to
five immutable private-ledger hashes and publishes terminal, privacy-safe maps
for all 1,007 attachment rows, 187 download rows, 10 archive instances, and 506
distinct archive-member hashes. The maps omit private identifiers, filenames,
URLs, local paths, UI text, and artifact bodies.

The 65 downloaded files are not treated as 65 repository-ready results. Each has its own terminal recommendation in the catalog and reconciliation ledger.

The structural receipt additionally maps all 95 messages, 1,127 attachment
rows, 1,490 Activity-entry assessments, 17,293 Activity file references, and
9,968 unique archive-member hashes without publishing private message IDs,
paths, URLs, filenames, or transcript bodies. Structural closure does not itself
grant semantic adoption.

## Private evidence excluded

The following remain local and are not publishable repository content:

- the full ChatGPT transcript;
- raw Activity text and HTML;
- browser HTML, screenshots, message IDs, session IDs, and UI locators;
- signed or expiring download URLs;
- user-uploaded private source files and verbatim source dossiers derived from them;
- conversation-native recovery files;
- bulk recovery archives and local extraction paths.

A public hash receipt establishes byte custody only. It does not establish correctness, source permission, originality, mathematical validity, or owner adoption.

## Superseded exact versions

Exact earlier bytes remain evidence but are not current authority:

- the pre-repair T299 and T300 payloads;
- theorem-origin registries V3 and V4 superseded by V5;
- the exact-active-identity registry V2 superseded by later identity custody;
- the pre-Codex readiness report and 42-payload audit superseded by V8 recovery;
- historical campaign-state snapshots superseded by later segment and merged-main state.

No wording is merged across versions.

## Historical identifier collision

The earlier P236-P242 Activity surface emitted a finite positive-Horn truth
guard as `AR8R-T236`, but V5 assigns `AR8R-T236` to a different authenticated-
support truth-factorization theorem. The earlier 1,408-byte payload is therefore
preserved under `AR8R-HR-P236P242-T236-v1`. It does not replace or renumber V5,
and it receives no new theorem-origin, novelty, adoption, or meniscus credit.
The adjacent earlier `AR8R-T235` payload is recorded only as a collision and
overlap tombstone because it substantially overlaps current `AR8R-T230` while
V5 assigns `AR8R-T235` to a different complete-lattice theorem.

## Deferred for incomplete audit

Candidate E and Candidate G remain research programs rather than adopted theorem records. Their historical packets explicitly lack a counted independent cold-audit gate. Mathematical plausibility, executable examples, or later thematic reuse do not repair that custody defect.

The tensor/representation program retains seven search controllers A-G and six
representation candidates A-F. They are different complete enumerations;
controller G is not a missing representation G. P186 completed an underpowered
diagnostic and implementation repair, while its prospective representation gate
remains unrun.

## Unresolved historical identities

The V8 authority remains controlling:

- exact historical recoveries: AR8R-T130 through AR8R-T143 and AR8R-T351 through AR8R-T354;
- AR8R-T354: exact payload, but `BLOCKED_FORMAL_DEFECT` and not repository-ready;
- AR8R-T150: role-preserving replacement only; original bytes unavailable;
- AR8R-T366: post hoc semantic reconstruction only; original bytes unavailable;
- 22 identities remain without exact payload: AR8R-T144 through AR8R-T149, AR8R-T151 through AR8R-T155, AR8R-T307, and AR8R-T309 through AR8R-T318.

No V11 program summary supplies historical payload wording for those identities.

## Post-merge proposals awaiting adoption

The authenticated continuation thread supplied exact public-safe packets for
PMR-001 through PMR-006 and a sanitized PMR-007 proposal through Round 20 V2.
These packets improve repository memory but do not self-authorize adoption.

All ten PMR-007 results remain new post-merge scoped proposals with historical
identity `NONE`, external review `OPEN`, owner adoption `PENDING`, and repository
readiness `EXTERNAL_REVIEW_REQUIRED`. Round 14 V1 remains blocked; Round 18 V1
and V3 and Round 20 V1 remain rejected evidence. Six mutable historical-link
drift incidents are separately recorded and never used as hash authority.

The three earlier source selections use exact bytes returned by individual
visible file cards. Their companion checkpoint archives contain eight same-name
byte conflicts, six selected files absent by the same basename, and four
multiple-basename cases. No wording is merged across those versions. The
visible-file-card versions are the bounded public selection; all archive hashes
and conflict relations remain explicit and unresolved in the provenance
manifests.

## Publication rule

A row may enter a public theorem packet only when exact evidence identifies its payload or bounded post hoc status, its audit and repair custody is sufficient, its source and novelty ceilings are explicit, and its repository owner is unambiguous. Otherwise it remains summarized, deferred, blocked, superseded, or private-excluded.

`MATERIALIZED_PUBLIC_PROPOSAL_NOT_ADOPTED` means that the row now has a public
repository owner. It does not mean that its theorem, proposal, terminology,
empirical result, metaphysical claim, or research program has been adopted.

## Task 7 additions

- Candidate E and Candidate G exact source bytes are now publicly custodied, but both remain `DEFERRED_INSUFFICIENT_AUDIT`; byte recovery does not supply the missing counted cold-audit chain.
- All ten PMR-007 results retain external review `OPEN`, owner adoption `PENDING`, historical identity `NONE`, and proposal-only status.
- Nonallowlisted public-safe deep-context members remain local pending separate review; no broad packet import is authorized.
- The exact TAC/SAC packet, acronym expansion, listed historical terms, and occurrence-meaning definition remain blocked by insufficient evidence. The V11 typed registry is `NEW_V11`, not historical recovery.
- Lean parsing, elaboration, kernel checking, axiom reporting, and machine-proof claims remain blocked on exact receipts.
- Ten Advances retains `CONFLICTING_38_42`, the missing declaration and path mismatch, and no local build/kernel authority.
- The Connes-rigidity dispute remains `UNRESOLVED_PENDING_OPERATOR_ALGEBRA_SPECIALIST_REVIEW`; the positive Nielsen proof is blocked by fatal gaps, while the presented critique does not refute the exact public OpenAI object.
- The supplied private manuscript, extracted text, rendered pages, contact sheets, and local audit artifacts are excluded. Only a public-safe hash/size/page receipt and public-source links are retained.
