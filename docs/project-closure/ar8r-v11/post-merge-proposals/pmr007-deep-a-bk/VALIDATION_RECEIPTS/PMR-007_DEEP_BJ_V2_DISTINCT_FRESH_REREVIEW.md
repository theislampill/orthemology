# PMR-007 Deep Round BJ V2 — distinct fresh rereview

```text
candidate: PMR-007-TQDC-1 V2
rereview relation: distinct same-program procedural lane
primary decision procedure reused: false
rereview method: partition-lattice enumeration plus independent anti-unification witnesses
disposition: PASS_WITH_NONBLOCKING_REDUCTION_AND_AUTHORITY_NOTES
external independence: false
```

## Frozen-byte custody

```text
frozen hash rows checked: 5
hash mismatches: 0
```

## Independent finite checks

The rereview enumerated set partitions rather than representation/target maps.
It tested partition refinement as the exact decoder criterion, partition meets
for target families, attained subdirect images, the full-product intersection
criterion, and deterministic-coarsening noncreation.

```text
partition pairs:
2,959

descent/refinement failures:
0

joint partition pairs:
2,959

meet failures:
0

subdirect failures:
0

full-product characterization failures:
0

coarsening triples:
19,583

coarsening failures:
0
```

## Anti-unification checks

All four independently encoded controls passed:

```text
full-support probability matrices with different matrix/nonnegative rank;
identical vertex partition with zero versus nonzero cycle holonomy;
set equivalence that is not a Z3-addition congruence;
equal one-bit marginals with different full joint law.
```

These controls confirm that the deterministic set-level descent layer does not
compute convex latent width, path exactness, algebraic quotients, or stochastic
interaction without their native extra structure.

## Rereview conclusion

The repaired packet correctly establishes one common deterministic extensional
layer and correctly withholds a reductive common calculus. The result is a
standard quotient/fibre application with zero general mathematical novelty.
It changes the central program by making the anti-unification burden exact:
future integration claims must preserve native typed operations and cannot
obtain credit by encoding the desired conclusion as a target label.
