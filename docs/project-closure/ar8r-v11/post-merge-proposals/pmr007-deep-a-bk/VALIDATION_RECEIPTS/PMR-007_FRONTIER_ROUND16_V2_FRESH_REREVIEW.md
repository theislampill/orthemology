# PMR-007 Frontier Round 16 V2 fresh rereview

```text
review relation:
same-model procedural rereview over frozen repaired hashes;
separate path and direct temporal implementation;
not external human review;
not independent model-lineage confirmation

disposition:
PASS_WITH_NONBLOCKING_ANCESTRY_AND_AUTHORITY_NOTES
```

## 1. Frozen and evidence custody

All six repaired-packet hashes matched. Six exact post-merge evidence owners
also matched their recorded hashes:

```text
AR2/AR3 collective-backbone reconciliation;
PMR-AGCOM-01;
PMR-AGCOM-02 V2;
PMR-AGCOM-06 V2;
PMR-LANG-01 V2;
PMR-LANG-02 V2.
```

The original standalone RP-T2 and RP-T40 packets remain unavailable in the
surfaced local bytes. Ancestry is therefore admitted only through the exact
indexed reconciliation.

## 2. Independent mechanisms

The rereview did not import the primary V2 checker. It independently used:

```text
explicit reason-node and dependency-edge boundary transport;
actual discharged-obligation flow;
path-level semantic/root/authority/invalidator checks;
complete memoryless-strategy enumeration for every one- and two-state game;
Tarjan SCC direct all-path semantics for CORE_ENTRY and co-Büchi objectives;
and direct recomputation of the stale-action witness.
```

## 3. Transport result

The positive two-edge path passed. The repaired regressions failed exactly as
required:

```text
VCT-F01:
preserved-obligation-not-discharged

VCT-F02:
reason-dag-boundary-mismatch
```

Thus the two cold-audit findings are closed at V2's declared finite scope.

## 4. Temporal result

Direct strategy semantics checked:

```text
labelled games: 444
restricted-menu pairs: 3,252
memoryless strategy evaluations: 7,863
monotonicity failures: 0
```

The exact stale-eligibility witness reproduced:

```text
v1: W_core = W_coB = {q0,q1}
v2: W_core = W_coB = {q0}
```

The same repair bytes remain present; their target-version applicability and
therefore action eligibility changed.

## 5. Admission

```text
LANG-VERSION-01:
ADMITTED_POST_MERGE_SUBORDINATE_APPLICATION

VCT-1:
ADMITTED_POST_MERGE_SCOPED_RESULT

VCT-2 countermodel family:
ADMITTED_POST_MERGE_SCOPED_CONTROL

VAM-1:
ADMITTED_POST_MERGE_SCOPED_RESULT

VST-1:
ADMITTED_POST_MERGE_SCOPED_RESULT
```

Credit remains:

```text
general mathematical novelty: 0
historical theorem identity: NONE
AR3 relation: specialization/application through indexed reconciliation
external review: OPEN
owner adoption: PENDING
repository proposal: eligible only as a scoped proposal after owner review
```

## 6. Program effect

The round materially changes the Candidate-A/B interface:

```text
reason-preserving communication supplies versioned action eligibility;
version or source drift can remove an action without changing its bytes;
a removed action can shrink both invariant-core and co-Büchi winning regions;
therefore temporal certificates require action-custody and model-digest reread.
```

It does not solve:

```text
distributed path discovery under private views;
common knowledge;
fault/dynamic-membership synthesis;
real daee successor-model completeness;
source truth;
world-directed metaphysics;
or Fusha/Qamus implementation.
```

```text
integrated champion: NONE
meniscus: MENISCUS_NOT_REACHED
natural closure: NOT_REACHED
```
