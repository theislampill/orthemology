# PMR-007 Deep Round AG V2 — strengthened R5 neutral common model through Deep AE

```text
identity: PMR-007-R5NR-1
round: PMR-007-DEEP-AG
version: V2
status: REPAIRED_POST_MERGE_RESEARCH_CANDIDATE
historical_identity: NONE
repository_mutation: NONE
central_task: strongest-current-neutral-rival common-model test
```

## 1. Exact selected union `U_AE*`

`U_AE*` is a finite dependency-closed selection of the strongest neutral
coordinates admitted or carried through Deep AE. It is not every neutral claim
in the repository.

```text
U1  process-relative underived/order placeholder;
U2  externally actual impersonal realizer placeholder;
U3  carried target-key authority and world-adequacy guards;
U4  carried truth-linked semantic-anchor coordinate;
U5  six-coordinate common orthing product object;
U6  registered reason-variable mediation and nuisance invariance;
U7  causal score/reason guidance in a frozen SCM;
U8  fittingness-table policy conformity;
U9  represented asymmetry resource;
U10 exact additive norm potential;
U11 connected nonseparable interaction family;
U12 global W/P/K role-proxy availability without required common bearer;
U13 typed links among order, reason, norm, selection, realization, and outcome.
```

`U1–U4` are carried premises in the witness, not newly derived or authenticated.

## 2. Finite witness

Use the state cube

\[
Q=\{0,1\}^3,
\qquad q=(m,r,n),
\]

with registered asymmetry `theta=m`, reason/semantic variable `r`, and nuisance
proxy `n`. Freeze the independent action-admissibility relation

\[
H(	heta,r)=\{r\oplus	heta\}.
\]

The impersonal policy is

\[
A(m,r,n)=r\oplus m,
\]

so it conforms to `H`, responds to interventions on `r` and `m`, and is
invariant under `n`.

Define the exact additive potential

\[
N(m,r,n)=m+r+n
\]

and a cross-domain function

\[
F(m,r,n)=mr+rn.
\]

The interaction supports `{m,r}` and `{r,n}` yield one connected additive
component. Thus the witness is not a fully independent additive product.

The formal bearers are:

```text
o — order-role placeholder;
a — impersonal finite realizer;
b,c — additional role placeholders.
```

Assign Knowledge-role and Power-role proxies to `a`, Will-role proxy to `b`, and
no common W/P/K-role bearer. These are functional proxies, not Divine
attributes.

## 3. R5NR-1A — selected-union satisfiability

The finite structure satisfies every `U_AE*` coordinate while setting

```text
PERSONAL = false;
INTENTIONAL_UPTAKE = false;
FIRST_PERSON_OWNERSHIP = false;
BECAUSE_F = false;
WISDOM = false;
SPEECH = false;
REVELATION = false.
```

Therefore the selected neutral union does not entail any of those targets in
the declared formal model class.

### Proof

The state table directly satisfies the policy, intervention, nuisance,
potential, and interaction equations. The carried guards are assigned true,
and the formal link graph is interpreted. The impersonal expansion satisfies
all neutral axioms and falsifies every personal/source target. It is therefore a
countermodel to the corresponding neutral entailments. ∎

## 4. R5NR-1B — repaired rival after Deep AE

Deep AE excludes a fully independent additive plural model when `b_add=1`.
The surviving R5 rival is therefore repaired to:

```text
necessary/underived abstract-order placeholder
+
externally actual impersonal powers-realizer placeholder
+
carried semantic, authority, and world coordinates
+
registered reason mediation and fittingness policy
+
nonseparable cross-domain law
+
plural or distributed role realization.
```

The rival's cross-domain law is real in the formal sense; it is not merely one
carrier containing freely recombinable features. It remains impersonal because
nothing in `U_AE*` implicitly defines the personal targets.

## 5. R5NR-1C — conservative personal twin

A second expansion keeps the complete `U_AE*` reduct fixed and sets the
personal, ownership, because-of-fittingness, Wisdom, and Speech predicates true.
The expansions therefore instantiate Deep AF's reduct-definability frontier:
current neutral structure does not select between them.

This is exact formal nonentailment, not equal probability or metaphysical
possibility.

## 6. Mandatory countermodels and deletion controls

```text
AG-CM1 CARRIED-PREMISE CONTROL:
  deleting target authority or world adequacy removes the corresponding
  interpretation without affecting finite consistency.

AG-CM2 FALSE-SEMANTIC CONTROL:
  the same transition table can be reinterpreted under a false semantic anchor;
  token response is not truth or uptake.

AG-CM3 ADDITIVE-RIVAL DELETION:
  deleting the cross terms restores independent additive modules, showing why
  Deep AE materially strengthened the rival.

AG-CM4 HIDDEN-LATENT CONTROL:
  an omitted latent may decompose the observed interaction; RESOURCE_COMPLETE
  remains load-bearing.

AG-CM5 ROLE-DISTRIBUTION CONTROL:
  all W/P/K proxies are globally available but no one bearer has all three.

AG-CM6 SOURCE-EXPANSION CONTROL:
  an authenticated Track-N contract may exclude the neutral impersonal
  expansion source-relatively; no neutral migration follows.

AG-CM7 IMPLEMENTATION CONTROL:
  a finite table satisfying the formal contract is not an implemented daee,
  OSM, PRH, human, or fiṭrī restoration process.
```

## 7. Bridge ledger effects

```text
underived order -> necessary concrete personal being:
  NOT ADVANCED; order and realizer are carried and may remain split;

actualizer -> agency/personality:
  COUNTERMODELED AT SELECTED FORMAL UNION;

truth-linked norm and causal reason mediation -> intentional uptake:
  COUNTERMODELED;

nonseparable cross-domain law -> one personal ground:
  COUNTERMODELED;

global W/P/K proxies -> common bearer or attributes:
  COUNTERMODELED;

articulability/communication -> Speech/revelation:
  NOT ENTAILED;

Track-N source predicates:
  MAY FILTER SOURCE-RELATIVELY UNDER THEIR OWN AUTHORITY GUARDS.
```

## 8. Source, world, and implementation ceiling

The witness does not derive the truth of the carried semantic, authority, or
world coordinates. It does not establish that its formal rival is a
metaphysically possible world. It does not refute a personal ground. It shows
only that the current selected neutral premises are jointly satisfiable in an
impersonal formal expansion.

## 9. Ancestry and novelty

```text
Deep R R5 common model:
  DIRECT ANCESTOR

Deep X–AE:
  STRICT STRENGTHENING OF THE REGISTERED NEUTRAL UNION

Deep AF:
  CONSERVATIVE-EXPANSION FRONTIER INSTANTIATION

general mathematical novelty:
  0

historical identity:
  NONE
```

The scientific gain is rival strengthening and exact union-level burden
localization, not a new general theorem.
