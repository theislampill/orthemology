> **Custody status:** `SEMANTIC_REISSUE_OR_REGENERATED_CUSTODY_COPY`  
> **Original byte identity:** not proven.  
> **Basis:** exact surviving source bytes where identified, plus the verbatim owner-visible Deep BL–BV boundary response in this authenticated conversation.  
> **Scientific effect:** none; this file preserves the prior disposition and does not rederive or alter it.

# PMR-007 Deep BV V2 distinct fresh rereview — semantic custody reissue

```text
method relation: distinct exact-partition procedure
primary-checker reuse as sole oracle: false
frozen-hash mismatches: 0
fresh_rereview: PASS_WITH_NONBLOCKING_MODEL_CLASS_AND_OPERATIONALIZATION_NOTES
```

The owner-visible receipt records that the distinct rereview used an independent exact-partition procedure rather than the primary checker’s classifier-map enumeration.

Recorded exercised scope:

```text
primary deterministic models: 8
primary stochastic models: 250
primary random TV trials: 100000
distinct exact profiles: 125
distinct exact models: 250
distinct random TV trials: 150000
distinct random intended-model classes: 50000
frozen-hash mismatches: 0
```

Nonblocking notes retained model-class relativity and the burden of independently eligible cross-architecture operationalization. The original rereview prose and implementation are unavailable; this record does not claim exact-byte recovery or independent rerun.
