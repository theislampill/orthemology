# Specialist A intake correction

The exact 77-file source tree is public-safe proposal custody, not current
theory. Independent intake reproduced the twelve finite Python checkers and the
separate rereview at their declared constructed scope. It also found defects
that prevent scientific admission.

## Blocking mathematical correction

Rounds R7 and R9 state an unrestricted full-codomain decoder equivalence. Their
converse needs an off-range extension guard. If the source is empty, the
observation codomain has an unattained element, and the target codomain is
empty, fibre constancy is vacuous but no total decoder exists.

Repair must do one of the following without changing the preserved source:

- factor only through the attained image;
- require an inhabited target codomain;
- require a nonempty source with a usable default; or
- require surjectivity onto the decoder domain.

Until a repaired candidate is frozen and independently reviewed, R7 and R9 are
`MATHEMATICAL_CORRECTION_REQUIRED`.

## Checker and audit limits

- the reported 79/79 combines about 72 computed negative-control predicates
  with seven hard-coded scope assertions; it is not 79 executed source mutants;
- the aggregate runner can accept stale result files unless outputs are removed
  before execution;
- R1 computes a decoder-source-leak list without making a nonempty list fatal;
- R12's 512 and 256 values count different coordinate conventions and must be
  named separately;
- the sanitized cold-audit/repair chain omits the failed auditor bytes and
  broader private context, so it is incomplete public audit custody;
- `CENTRAL_INTEGRATOR_RECEIPT.yaml` has two unresolved locators and is not a
  standalone repository authority.

## Lean disposition

Both original Lean files failed because their imports followed module
documentation commands. Separately preserved intake repairs pass standalone
Lean 4.32.2 checking. The observation file formalizes strategy factorization,
not the R4 co-Büchi theorem. See `intake-review/LEAN_INTAKE_RECEIPT.json`.

Current ceiling:

```text
custody: PUBLIC_SAFE_PROPOSAL_SOURCE_CUSTODY_ONLY
historical_identity: NONE
repository_scientific_adoption: NONE
owner_adoption: PENDING
external_review: OPEN
general_novelty: 0
mathematical_status: CORRECTION_REQUIRED_R7_R9
Lean_project_integration: NOT_RUN
meniscus: MENISCUS_NOT_REACHED
natural_closure: NOT_REACHED
```
