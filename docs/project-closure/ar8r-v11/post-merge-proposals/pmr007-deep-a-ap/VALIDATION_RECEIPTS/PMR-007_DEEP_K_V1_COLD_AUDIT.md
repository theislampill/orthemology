# PMR-007 Deep Round K V1 cold audit

```text
audited candidate: PMR-007-WFB-1-CANDIDATE-V1
frozen receipt: PMR-007_DEEP_K_V1_FROZEN_HASHES.sha256
disposition: REPAIR_REQUIRED
```

## Blocking findings

### DK-F01 — wisdom was conflated with successful execution

V1 makes capacity and actual execution necessary for `WisdomEpisode`. A knowledgeable agent can choose an objectively fitting end and appropriate means yet be externally prevented from acting. The failure is practical success, not necessarily lack of wisdom. Split:

```text
wise-directed deliberation/choice;
effective execution of a wise-directed choice;
and stable Wisdom as a bearer-level disposition.
```

### DK-F02 — impersonal representation was mislabeled Knowledge

`K-CM2` marks `K=true` for an impersonal truth tracker even though V1 defines `K` as a bearer's understanding. This silently imports mentality into the rival. Separate:

```text
REP: truth-correlated representation or discrimination;
K: agentive understanding/knowledge.
```

The impersonal rival may have `REP` and reliability while `AG=K=W=false`.

### DK-F03 — the positive result is a stipulated role contract, not an existence or reduction theorem

`AG∧K∧F∧W∧M...` is sufficient for a defined role because the role was specified by those coordinates. The substantive results are the nonentailments and guard independence. The repaired packet must not call the conjunction an independently proved metaphysical Wisdom theorem.

### DK-F04 — one episode does not establish a stable Wisdom attribute

Even a wise-directed episode can be accidental relative to the bearer's standing dispositions. Attribute attribution requires a stability, ownership, or counterfactual-disposition guard over an appropriate episode class. Add this as a separate, unestablished bearer-level bridge.

### DK-F05 — source custody is asserted but not frozen

The source paragraphs are relevant, but V1 supplies no byte hashes or executable locator receipt. Freeze the supplied English source bytes and exact locator phrases. Preserve the roles:

```text
Asfahani: translated-primary access;
al-Ujayri: contemporary Athari synthesis;
neutral entailment: none.
```

## Nonblocking notes

1. Proper function, fittingness, agency, common bearer, and Wisdom are correctly separated in the negative models.
2. The Asfahani passage itself distinguishes praiseworthy and blameworthy instances of speech and volition; it supports the guard architecture source-relatively.
3. General mathematical novelty is correctly zero.

## Required repair

```text
split REP from K;
split wise direction from effective execution;
reserve stable Wisdom for a further disposition/ownership guard;
reclassify the positive package as a typed role contract;
fix source custody and locators;
run a distinct model/guard and source rereview.
```
