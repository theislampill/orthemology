> **Custody status:** `SEMANTIC_REISSUE_OR_REGENERATED_CUSTODY_COPY`  
> **Original byte identity:** not proven.  
> **Basis:** exact surviving source bytes where identified, plus the verbatim owner-visible Deep BL–BV boundary response in this authenticated conversation.  
> **Scientific effect:** none; this file preserves the prior disposition and does not rederive or alter it.

# PMR-007 Deep BV — common-intervention identifiability (V2 semantic custody reissue)

```text
identity: PMR-007-CIID-1
status: ADMITTED_POST_MERGE_SCOPED_CENTRAL_INTERVENTION_IDENTIFIABILITY_RESULT
historical_identity: NONE
general_mathematical_novelty: 0
owner_adoption: PENDING
external_review: OPEN
```

## Typed setting

Let `M` be a finite declared model class, let `Q : M → QVal` be the architecture coordinate under investigation, let `J` be a frozen family of **typed common interventions**, and let `E_J(m)` be the complete profile of output laws induced by those interventions in model `m`.

## Exact scoped characterization

The coordinate `Q` is exactly identifiable from the declared experiment profile if and only if it is constant on every complete profile fibre:

```text
for all m,m' in M,
E_J(m) = E_J(m')  implies  Q(m) = Q(m').
```

Equivalently, `Q` factors through the map `E_J` on the declared finite model class.

## Pairwise versus class-wide discrimination

One personal model and one impersonal model may differ under one intervention while `Q` remains unidentified over the full model class. Positive total variation for one selected pair is therefore weaker than class-wide identification and does not establish model truth, actual-world selection, or a canonical prior.

## Constitution-sensitive intervention firewall

A disputed internal variable—such as `IntentionalUptake`, `FirstPersonOwnership`, `Personality`, or `Wisdom`—cannot be treated as neutral evidence merely because it appears in the preferred architecture's vocabulary. An eligible common intervention requires:

1. one independently specified abstract intervention type;
2. a model-specific implementation in every compared architecture;
3. preservation of intervention semantics;
4. comparable output semantics;
5. source, version, and authorization eligibility when relevant; and
6. no target-coordinate-dependent implementation choice.

A target-dependent or semantically mismatched operationalization is a hidden anchor rather than an architecture discriminator.

## What this establishes

The result closes the finite, model-class-relative identifiability characterization for a frozen common-intervention experiment.

## What this does not establish

It does not provide an actual experiment that separates the unified-personal, impersonal-powers, and plural architectures. It does not establish personality, intentionality, Wisdom, actual-world truth, source-to-world applicability, or metaphysical impossibility of an impersonal rival.

## Custody limitation

The original theorem file is unavailable. This record reconstructs only the theorem and scope printed in the authenticated owner-visible Deep BL–BV boundary response. It is not an exact-byte recovery and contains no claim about omitted proof wording.
