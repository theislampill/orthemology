# Private-data exclusion report

External file-library report bytes, uploaded source books, transcripts, browser artifacts, absolute local paths, signed URLs, and credentials are excluded. Exact excluded-file rows and reasons are in the JSON report. A final byte scan is performed after tree freeze.
