# Deferred Candidate E/G and integration-map source

This directory preserves 24 selected historical source files byte-for-byte: 11 Candidate E files, 10 Candidate G files, and three earlier A–N / M–T–N integration maps. The files below `exact/` are evidence, not current authority. They are protected by a `-text` rule and must never be normalized, reformatted, annotated, or edited in place.

Candidate E and Candidate G remain `DEFERRED_INSUFFICIENT_AUDIT`. The integration maps are `EARLIER_PROGRAM_EVIDENCE`; current status is controlled by the V11 architecture, bridge, matrix, and provenance owners outside this exact subtree. No selected file supplies owner adoption, theorem-origin credit, a Lean/kernel claim, an integrated champion, a meniscus, or natural closure.

The allowlist contains exactly 24 files and 84,163 bytes. Its file-level hashes, source-era classifications, and exclusions are recorded in `../../provenance/AR8R-DEEP-CONTEXT-SOURCE-RECEIPT-V11.yaml`.

Excluded material includes the packet manifest, path-bearing boundary and necessary-being records, steering material, private corpus content, and every nonallowlisted member. None belongs in the public repository.
