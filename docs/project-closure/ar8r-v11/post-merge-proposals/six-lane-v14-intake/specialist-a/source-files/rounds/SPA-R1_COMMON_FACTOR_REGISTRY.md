# SPA-R1 — typed common-factor registry and declared-manifest coverage

**Status:** specialist-local research only; no repository adoption, theorem ID,
novelty, architecture implementation, or metaphysical conclusion.

## Frozen object

The object is a finite eight-factor interface over three *constructed signature
witnesses* named `unified_personal`, `impersonal_powers`, and `plural`. The names
index the three rival coordinates; they do not certify that the fixture
implements any real member of those metaphysical architecture classes.

The factor set is:

```text
causal_mode
target_truth_relation
form
content
authority_version
provenance_operation
resource_band
challenge_nonce
```

Every factor has two values, producing 256 cells. Every architecture witness has
a total typed map for all eight factors. The common decoder has no architecture
or target argument, and the policy registry forbids architecture, target,
private-role, and source-world fields.

## Result

The executable validator establishes only the following bounded claim:

> The supplied finite fixture is total over its declared factor and architecture
> witness manifest, its 256-cell map is the exact Cartesian product, its decoder
> and policy interfaces pass the declared target-blind syntactic guards, and its
> 22-family coverage map is total over the separately frozen 22-family manifest.

This is not an implemented common intervention across actual architectures.
It repairs Challenger A's missing machine-readable registry and total-coverage
surface only at the declared-signature level.

## Finite and measurable contracts

The finite contract freezes finite alphabets, two resource bins, horizon two,
complete transcripts, and candidate-independent policies. Continuous or
unbounded fields are prohibited under this theorem.

The measurable contract is schema-only. It requires locators and proof receipts
for the input/output measurable spaces, kernel, decoder, adaptive policy, and
target blindness. None is supplied or machine verified here. No finite result is
silently generalized to measurable spaces.

## Negative controls

Seven mutants are killed:

1. missing architecture-factor map;
2. decoder target/architecture leakage;
3. an unbounded resource coordinate inside the finite theorem;
4. target-dependent policy input;
5. missing decoder measurability receipt;
6. one deleted factorial cell;
7. incomplete declared-family coverage.

## Source relation

This operation consumes the Deep BV common-profile criterion, Deep BW-CJ
factor/anchor/resource/source-world warnings, and Challenger A's independent
review. It does not change their adoption or novelty status. The 22-family map
is explicitly not a whole-repository exhaustion claim.

## Conclusion ceiling

```text
TYPED_FINITE_DECLARED_SIGNATURE_FIXTURE_ONLY
NO_ARCHITECTURE_IMPLEMENTATION
NO_SOURCE_WORLD_TRANSFER
NO_METAPHYSICAL_IDENTIFICATION
```
