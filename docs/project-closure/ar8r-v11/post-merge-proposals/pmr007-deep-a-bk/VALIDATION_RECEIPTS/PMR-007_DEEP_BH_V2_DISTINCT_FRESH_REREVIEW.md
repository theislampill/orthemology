# PMR-007 Deep Round BH V2 — distinct fresh rereview

```text
identity: PMR-007-TCRPF-1
candidate version: V2
review relation: same-program distinct procedural rereview; not external review
primary checker imported: false
disposition: PASS_WITH_NONBLOCKING_TRUTH_AUTHORITY_AND_FUNCTION_SCOPE_NOTES
```

The rereview used an independently implemented componentwise path-integration
method and did not import the primary checker.

```text
frozen hash rows:
6

hash mismatches:
0

exhaustive three-vertex graph/field/loss cases:
1,728

equivalence failures:
0

random possibly disconnected cases:
60,000

random equivalence failures:
0

proper-function account expansions:
1,024

dynamic-registry countermodel:
PASS

ordinal/additive controls:
PASS

source-scope checks:
PASS
```

The componentwise forest calibration plus fundamental-cycle condition is
exactly equivalent to global truth-loss calibration over the frozen registry.
A new edge can invalidate a previously valid certificate. Additive calibration
is stronger than ordinal acyclic rankability.

The theorem does not warrant the truth-loss coordinate or select a
function-fixing account. The fiṭrah account remains Track-N/source-relative;
selected-effect, design, causal-role, organizational, teleological, and
Plantingian accounts require their own evidence.

```text
fresh rereview:
PASS_WITH_NONBLOCKING_TRUTH_AUTHORITY_AND_FUNCTION_SCOPE_NOTES

external review:
OPEN

general mathematical novelty:
0
```
