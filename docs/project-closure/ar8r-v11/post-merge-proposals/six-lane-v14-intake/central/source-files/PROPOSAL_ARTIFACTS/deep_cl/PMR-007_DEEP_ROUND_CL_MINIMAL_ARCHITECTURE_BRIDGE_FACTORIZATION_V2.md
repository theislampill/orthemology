# Deep CL V2 — registry-relative architecture bridge factorization and the surviving U–I residual

## Identity and status

```text
identity: PMR-007-MABF-1
canonical version: V2
status: ADMITTED_POST_MERGE_SCOPED_CENTRAL_ARCHITECTURE_BURDEN_RESULT
historical identity: NONE
repository scientific adoption: NONE
owner adoption: PENDING
external review: OPEN
general mathematical novelty: 0
```

`ADMITTED` means admitted in this Pro research lane at the frozen scope below. It is not repository adoption, historical recovery, an actual-world architecture verdict, or meniscus credit.

## 1. Finite conflict-cover criterion

Let `M` be a finite declared model class, `Q:M→C` the target architecture coordinate, and `A` a frozen family of typed anchors. For `a∈A`, let `a(m)` be its value on `m`. A selected anchor family `S⊆A` identifies `Q` exactly when the joint anchor map

```text
m ↦ (a(m))_{a∈S}
```

has fibres contained in `Q`-fibres.

Equivalently, `S` identifies `Q` iff every conflict pair

```text
{m,m'} with Q(m) != Q(m')
```

is separated by at least one `a∈S`.

This is the standard finite fibre-constancy/conflict-cover mechanism. It is an application of the Deep-BX/Deep-BV/AR8R-T294 family and receives no independent general theorem-origin or novelty credit.

## 2. Concrete U/I/P factorization

The frozen architecture surface is:

```text
U — unified personal;
I — one-bearer impersonal powers;
P — plural coordinated.
```

The complete certified neutral experiment induces one block:

```text
{U,I,P}.
```

It therefore resolves none of the three architecture-conflict edges.

The formal common-bearer coordinate induces:

```text
{U,I} | {P},
```

and resolves exactly `UP` and `IP`.

The formal intentional-uptake coordinate induces:

```text
{U} | {I,P},
```

and resolves exactly `UI` and `UP`.

Their partition meet is discrete:

```text
{U} | {I} | {P}.
```

Within the **declared formal-coordinate registry**, the pair

```text
{common_bearer_coordinate, intentional_uptake_coordinate}
```

is inclusion-minimal for identifying U/I/P. Each coordinate alone leaves one architecture collision.

This is not an evidence-cost theorem. It is a registry-relative partition fact. A later independently eligible direct three-way anchor would change the minimum.

## 3. Source-relative route

The full Deep-CK Track-N Wisdom role package has the same U-versus-I/P partition as intentional uptake only when all 19 source/world/Wisdom guards are supplied. In the declared Track-N conditional registry,

```text
{common_bearer_coordinate, track_n_wisdom_role_package}
```

is inclusion-minimal for classifying the three frozen candidates.

The Track-N package is composite, source-relative, and actual-world conditional. It is not one neutral observation, and its use does not migrate source predication into Track-T entailment.

## 4. Exact central residual

The current common-bearer coordinate excludes the strict plural realization `P` relative to the declared incidence semantics, but it leaves `U` and `I` together:

```text
UI residual:
one-bearer unified-personal versus one-bearer impersonal-powers.
```

Thus the strongest current architecture burden is not merely “establish one bearer.” It is:

> supply an independently warranted, target-blind, type-preserving anchor or enlarged experiment that separates `U` from `I`, or prove that the declared admissible anchor family leaves that conflict uncovered.

The current certified neutral experiment does not resolve `UI`. Intentional uptake is presently a target coordinate, not an independently operationalized neutral measurement. The source-relative role package can resolve the edge only conditionally on its full custody and world-link chain.

## 5. Type ceilings

```text
common bearer here:
one-world role co-instantiation;
not uniqueness, B_ID, transworld identity, derivational unity, or personality.

intentional uptake here:
a formal target coordinate;
not an eligible common intervention or observed neutral variable.

Track-N classification here:
conditional source/world role classification;
not actual-world selection or neutral proof.

U/I/P universe:
a declared comparison surface;
not an exhaustive ontology.
```

## 6. Strongest controls

1. **Direct target oracle:** one coordinate can identify U/I/P, demonstrating why minimality is registry-relative; the oracle is ineligible because it reads the target.
2. **Common-bearer-only control:** separates plural `P` but leaves U/I unresolved.
3. **Uptake-only control:** separates `U` but leaves I/P unresolved.
4. **Track-N composite control:** has U-versus-I/P force only conditional on the complete guard package.
5. **Registry-extension control:** an independently justified direct anchor could change the minimum; the theorem does not bar one.
6. **Architecture-exhaustion control:** even exact U/I/P classification would not prove that the actual world is exhausted by those candidates.

## 7. Program effect

```text
plural-realizer burden:
sharply localized to common-bearer co-instantiation;

personal/impersonal burden:
sharply localized to the UI cross-cut;

neutral common experiment:
insufficient for both burdens;

source-relative route:
conditional and composite;

strongest one-bearer impersonal rival:
SURVIVES;

integrated champion:
NONE;

meniscus:
MENISCUS_NOT_REACHED;

natural closure:
NOT_REACHED.
```
