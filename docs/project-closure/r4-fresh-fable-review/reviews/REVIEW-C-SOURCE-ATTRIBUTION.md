# Review C — source-attribution audit (fresh Fable recovery review)

Scope: source-status registry, sourcing ledgers, bibliography, companion
citation surfaces on the final review branch. Surfaced model:
`claude-fable-5`.

- **Bibliographic verification presented as wording verification**: none
  remaining. CIR-1 (bibliographic, `SECONDARY_VERIFIED` with explicit
  `status_scope`) is split from CIR-1W (wording, `COMPILATION_MEDIATED`,
  promotion trigger recorded, never load-bearing alone).
- **Compilation access presented as direct primary access**: none remaining.
  ATH-3 retained at `COMPILATION_MEDIATED` — this fresh session did not open
  a printed edition or authoritative full-text rendering and therefore made
  no upgrade; the [via compilation] agreement guard forbids recurrence and
  was tamper-probed live (restoring `PRIMARY_TEXT_EXACT` fails 3 checks).
- **Source/prose status contradictions**: guard green corpus-wide; the
  ASCII-fold closes the folded/unfolded mismatch that let the original
  contradiction survive. Accepted limitation (safe direction): generic work
  tokens (e.g. `sharh`) could false-flag a future different-work row, forcing
  a reconciliation rather than hiding one.
- **School-internal claims presented as neutral**: none found; the Atharī
  companion is school-labeled throughout, registry rows carry `school_flag`,
  the final-accounting table separates philosophical/revelational/school-
  internal bases with the absolute engineering firewall row, and kalām nafsī
  positions remain described neutrally.
- **Orphan citations**: none; single-bibliography rule enforced (stray-.bib
  check), both re-orphaning directions guarded, LAT keys require registry
  rows by DOI.
- **Source-status scope overclaim**: withdrawn and machine-enforced — the
  registry is authoritative only for its five declared claim families, with a
  `not_covered` block; scope enforced in both directions; a green validator
  run is stated to establish record shape and internal agreement, never
  worldly truth.

Blocking findings: **none**. Unresolved source triggers (carried, with
recorded promotion conditions): CIR-1W publisher full-text access; ATH-3
direct edition access (with RR-1 edition-dependent pagination); rows 31–35
classical edition verification (pre-existing residual, owner-adjacent lane).
