# PMR-007 Deep AR V3 distinct fresh rereview

```text
identity: PMR-007-PREC-1
frozen theorem version: V2
rereview harness: V3
V2 harness disposition: PRESERVED_FAILED_HARNESS
final disposition: PASS_WITH_NONBLOCKING_AUTHENTICATION_AND_WARRANT_NOTES
same-model procedural relation: yes
external review: no
```

## Preserved first failure

The first distinct harness returned six failures because it incorrectly required `L² != L` at `L=0`. That was a checker defect, not a theorem countermodel. The failed script and result remain in custody.

## Corrected independent method

The V3 rereview independently:

- verified all six frozen V2 hashes;
- recomputed common-channel data processing on a denominator-three binary probability grid;
- recomputed two-root likelihood factorization directly from joint distributions;
- tested faithful copy bundles up to five repeated copies;
- separated finite informative likelihood ratios from `0`, `1`, `∞`, and undefined support cases;
- exercised common-cause contraction and root-alias partitions.

It did not invoke the primary checker.

## Results

```text
frozen files checked: 6
hash mismatches: 0
binary distributions: 4
stochastic channels: 9
root/channel cases: 144
two-root factor cases: 1,024
copy-bundle cases: 160
common-cause boundary cases: 6
root-alias cases: 3
failures: 0
```

## Rereview judgment

The V2 theorem is valid at the declared finite probabilistic scope. Its load-bearing guards are:

```text
independently authenticated root partition;
one common candidate-independent channel;
conditional independence before likelihood multiplication;
common-support and finite-ratio guards;
no migration from machine provenance into truth or warrant.
```

## Nonblocking notes

- Root authentication remains external to the theorem.
- Conditional independence must be evidenced, not inferred from distinct labels, carriers, or paths.
- The theorem does not establish tawātur, source truth, recipient authority, or common knowledge.
