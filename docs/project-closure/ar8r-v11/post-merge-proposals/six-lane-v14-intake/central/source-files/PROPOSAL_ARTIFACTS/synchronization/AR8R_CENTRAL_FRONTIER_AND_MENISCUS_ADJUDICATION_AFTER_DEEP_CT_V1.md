# Central frontier and meniscus adjudication after Deep CT

The integrated frontier now has a sharper negative shape, not a positive ascent.

```text
neutral/common-profile partition: {U,I,P}
common-bearer partition:          {U,I}|{P}
formal uptake partition:          {U}|{I,P}
currently eligible neutral meet:  {U,I,P}
formal unrestricted meet:         {U}|{I}|{P}
```

The `U/I` residual survives objective truth-linked functional fit, common-bearer co-instantiation, complete frozen common histories, profile-measurable corrections, and partial-domain totalizations whose capability signal is not independently certified. `P` is excluded only under the exact one-world common-bearer premise; coordinated plural and multiple-realizer variants survive.

A positive personal-ground bridge now requires a **common, target-blind, bearer-local and episode-local because-of-uptake or ownership intervention**, with actual implementation maps in both U and I and a complete experiment profile. A target-defined availability bit, architecture oracle, source-relative label, successful output, or objective fittingness relation is not such an intervention.

The same residual recurs in proper function, Wisdom, noetic restoration, Speech/grammar, source-world transport, TAC/SAC provenance, and recipient authorization. The recurrence supports a vector-valued correction programme; it does not show that one scalar invariant or personal source is missing.

```text
integrated champion: NONE
meniscus: MENISCUS_NOT_REACHED
natural closure: NOT_REACHED
owner decision required: false
independent ready work remaining: true
```
