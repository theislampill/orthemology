> **Custody status:** `SEMANTIC_REISSUE_OR_REGENERATED_CUSTODY_COPY`  
> **Original byte identity:** not proven.  
> **Basis:** exact surviving source bytes where identified, plus the verbatim owner-visible Deep BL–BV boundary response in this authenticated conversation.  
> **Scientific effect:** none; this file preserves the prior disposition and does not rederive or alter it.

# PMR-007 Deep BV V2 repair log — semantic custody reissue

```text
repair_of: PMR-007 Deep BV V1
blocking_findings_repaired: 8 / 8
```

The V2 scope was repaired by:

- typing one abstract common-intervention family and requiring semantics-preserving model-specific implementations;
- replacing pairwise discrimination language with a full experiment-profile fibre characterization;
- restricting total variation to a pairwise statistical difference rather than architecture truth;
- permitting constitution-sensitive operationalization only under target-independent, type-preserving, semantics-preserving mappings;
- freezing the one-shot finite experiment scope and leaving adaptive extensions separate;
- declaring model-class relativity explicitly;
- preserving source, translation, world, version, and authorization firewalls; and
- separating formal intervention existence from resource, authorization, and physical implementability.

The original repair-log bytes are unavailable; this is a bounded reconstruction from the visible boundary response.
