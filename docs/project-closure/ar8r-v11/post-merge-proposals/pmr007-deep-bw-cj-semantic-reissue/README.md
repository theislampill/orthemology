# PMR-007 Deep BW–CJ semantic custody reissue

**Status:** `MATERIALIZED_PUBLIC_PROPOSAL_NOT_ADOPTED`

**Custody:** `SEMANTIC_REISSUE_OR_REGENERATED_CUSTODY_COPY`

This directory preserves all 40 public-safe files from
`AR8R_PMR007_REPOSITORY_UPDATE_PROPOSAL_THROUGH_DEEP_CJ_REISSUE_V1.zip`
(SHA-256 `215be89bdca4a33d4272182b2eb21d956290691ac148b1c8df92db9fde96a4af`).
The files under `source-files/` are byte-exact copies of that reissue package.
They are not the lost original Deep BW–CJ ephemeral files.

The surface records fourteen scoped proposal identities, from
`PMR-007-TCIS-1` through `PMR-007-FSGW-1`. Every identity retains:

- historical identity `NONE`;
- original historical or ephemeral bytes recovered `false`;
- general mathematical novelty `0`;
- owner adoption `PENDING`;
- external review `OPEN`;
- repository scientific adoption `NONE`.

Independent intake review found that the V1 checker initialized the Deep CI and
Deep CJ twin counters at 20,000 and zero failures without executing those twin
controls. Its top-line 14/14 PASS is therefore rejected as a distinct-body
verification. The V1 bytes remain frozen as evidence of that defect.

The first repository-created V2 intake checker executed the missing loops, but
its 20,000 repetitions merely reconstructed the desired fibre predicates and
did not provide independent evidence. It is preserved and explicitly rejected.

V3 retains BW–CH's bounded results and replaces CI/CJ with exhaustive checks of
their declared finite signatures, target-leak/profile-change/same-target
controls, explicit CJ fittingness-sensitivity and payoff-independence rechecks,
and three killed predicate mutants. This establishes only constructed finite
signature witnesses. BW–CH still lack adequate per-result independent
rereview, and CI/CJ remain conditional set-theoretic countermodels rather than
verified architecture implementations. No result is scientifically adopted by
this import.

Validate this surface with:

```text
python tests/test_pmr007_deep_bw_cj_semantic_reissue.py
python scripts/validate_pmr007_deep_bw_cj_semantic_reissue.py
```
