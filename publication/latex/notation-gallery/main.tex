% GENERATED FILE — DO NOT EDIT; authoritative prose is Markdown.
% source: docs/notation-gallery.md
% source-sha256: 449ead6c2f13ae066cf0ae20d3b08f76e498bf58c3c8b18da34e70a8d2863b49
% source-qualification: research-stage-draft
% source-qualification: not-peer-reviewed
% source-qualification: not-externally-peer-reviewed
% source-qualification: not-empirically-validated
\documentclass[10pt,letterpaper,twocolumn]{article}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{natbib}
\usepackage{xcolor}
\geometry{letterpaper,margin=0.75in}
\hypersetup{colorlinks=true,linkcolor=blue,urlcolor=blue,citecolor=blue}
\setlength{\emergencystretch}{3em}
\title{Orthemology notation gallery}
\author{}
\date{}
\begin{document}
\twocolumn[
\maketitle
\textbf{Status:} DRAFT — not peer reviewed. Rendered by the R7B mathematical- typesetting pipeline (Decision 0023). Every normative symbol in \texttt{docs/notation-registry.yaml} appears below as real mathematics: GitHub renders the \texttt{\char36{}...\char36{}} inline math and fenced \texttt{math} blocks through MathJax, and \texttt{scripts/build\_pdfs.py} renders \texttt{artifacts/notation-gallery.pdf} through the pinned Typst compiler via \texttt{scripts/latex\_to\_typst\_math.py}. This is a \textbf{typography layer, not a notation redesign}: each row's meaning is the registry role, unchanged (Decision 0005). \texttt{scripts/validate\_math\_source.py} asserts a bijection between non-retired symbols and this gallery and that \texttt{scripts/validate\_pdf\_math.py} finds no missing-glyph (notdef/tofu) in the rendered PDF — the defect reproduced in \texttt{docs/project-closure/r7b/R7B-PDF-MATH-BASELINE.md} (the core \texttt{mu}-vector and \texttt{C}-vector rendering as notdef under the old \texttt{\#raw} path). Below they compose correctly as $\vec\mu$ and $\vec C$.

\vspace{1em}
]

\section{1. Normative symbols rendered as mathematics}

\begin{center}
\begin{tabular}{@{}p{0.313\linewidth}p{0.313\linewidth}p{0.313\linewidth}@{}}
\toprule
\textbf{Registry key} & \textbf{Rendered} & \textbf{Role} \\
\midrule
\texttt{A} & $A$ & declared analysis (identifier + version) \\
\texttt{alpha, beta, ...} & $\alpha, \beta$ & actor indices \\
\texttt{T = task(A)} & $T = \operatorname{task}(A)$ & task component of the analysis \\
\texttt{M\_univ, O\_univ} & $\mathcal{M}, \mathcal{O}$ & universal occurrence and ortheme universes \\
\texttt{M\_A, O\_A} & $\mathcal{M}_A \subseteq \mathcal{M}, \ \mathcal{O}_A \subseteq \mathcal{O}$ & analysis-active domain and repertoire \\
\texttt{Inst\_A} & $\operatorname{Inst}_A \subseteq \mathcal{M}_A \times \mathcal{O}_A$ & analysis-relative instantiation (the primitive) \\
\texttt{O*(m; A)} & $O^*(m; A)$ & true (actual) profile of m under A \\
\texttt{Pi\_A} & $\Pi_A$ & complete-profile space under A \\
\texttt{Pi\_A\_partial} & $\Pi_A^\partial$ & partial-profile space under A \\
\texttt{C\_hat} & $\hat C_{A,\alpha,t}(m)$ & candidate set of complete profiles \\
\texttt{p\_hat} & $\hat p_{A,\alpha,t}(m) \in \Pi_A^\partial$ & inferred (partial) profile — belief, never ground truth \\
\texttt{Omega} & $\Omega_{A,\alpha,t}$ & observation map \\
\texttt{mu} & $\mu = \langle g, S_\mu, \operatorname{select}_\mu, \operatorname{prov}, \operatorname{ver} \rangle$ & metaortheme type \\
\texttt{pi\_mu} & $\pi_\mu$ & paired meta-policy \\
\texttt{mu\_bar} & $\bar\mu_{e,j}$ & the j-th metaorthemma in episode e \\
\texttt{MetaInst} & $\operatorname{MetaInst}(\bar\mu_{e,j}, \mu)$ & token typing of a metaorthemma under a metaortheme \\
\texttt{Gamma\_E} & $\Gamma_E = (E, \rightsquigarrow)$ & episode DAG \\
\texttt{e\_Gamma} & $e_\Gamma = \operatorname{comp}(\Gamma_E)$ & composed boundary-level episode \\
\texttt{GoalSchema} & $\operatorname{GoalSchema}(\alpha)$ & parametric goal schema \\
\texttt{G\_target} & $\mathcal{G}_{\alpha, A_\alpha}$ & actor-alpha's grounded target profile set \\
\texttt{Rep\_A} & $\operatorname{Rep}_A$ & representation family under A \\
\texttt{chi} & $\chi \in \operatorname{Rep}_A$ & one representation \\
\texttt{L\_A\_star} & $L_A^*(\chi)$ & best attainable loss/risk under representation chi \\
\texttt{select\_mu} & $\operatorname{select}_\mu$ & metaortheme state-selecting evidence procedure \\
\texttt{estatus\_e} & $\operatorname{estatus}_e$ & per-claim evidence-status map of episode e \\
\texttt{eps\_A} & $\epsilon_A$ & accepted tolerance of the analysis \\
\texttt{theta\_stop} & $\theta_{\text{stop}}$ & ANDON stop/escalation risk threshold \\
\texttt{K\_A} & $\mathcal{K}_A$ & cause repertoire \\
\texttt{R\_A} & $\mathcal{R}_A$ & route repertoire \\
\texttt{W\_A} & $\mathcal{W}_A$ & warrant-state repertoire \\
\texttt{Q\_e} & $\mathcal{Q}_e$ & claim ledger of episode e \\
\texttt{delta\_e} & $\delta_e$ & residual-disposition map of episode e \\
\texttt{ReqPath} & $\operatorname{ReqPath}(e)$ & governance-derived required pathway verdicts \\
\bottomrule
\end{tabular}
\end{center}

\section{2. Structural constructs (typesetting showcase)}
The pipeline handles the full range of publication constructs the corpus uses. These are the constructs named in the Decision 0023 design spike.


\subsection{2.1 Episode signature (display)}
The formal-core episode signature — the exact site of the reproduced notdef defect — rendered as real mathematics (the vectors $\vec\mu$, $\vec C$ compose):


\[
e = \langle \operatorname{id};\ m, \kappa, v;\ x, H;\ \alpha, w, A, T, t;\ \vec\mu, \operatorname{MetaTok}, \pi;\ \vec C, \hat p;\ r;\ \operatorname{estatus};\ \mathcal{Q};\ \delta;\ \operatorname{hand\_in}, \operatorname{hand\_out};\ a, \operatorname{Succ} \rangle
\]

\subsection{2.2 Multi-line aligned equations}

\[
\begin{aligned}
\operatorname{TokenAdequate}(\bar\mu, e) &\Leftrightarrow \operatorname{MetaInst}(\bar\mu, \mu) \wedge \operatorname{Compatible}(\bar\mu, A(e)) \\
\operatorname{V3c}(e) &\Leftrightarrow \forall \bar\mu \in \operatorname{MetaTok}(e): \operatorname{TokenAdequate}(\bar\mu, e)
\end{aligned}
\]

\subsection{2.3 Set comprehension and predicate}
Grounded target (a set of \textbf{profiles}, $\subseteq \Pi_{A_\alpha}$, not of occurrences): $\mathcal{G}_{\alpha,A_\alpha} = \{\, p \in \Pi_{A_\alpha} \mid \operatorname{GoalSchema}(\alpha)(p) \,\}$, and strict soundness $\operatorname{StrictlySoundReasoning}_\chi(e) := \operatorname{ReasoningPathAdequate}_\chi(e) \wedge \operatorname{TokenTruthLinked}_\chi(e)$.


\subsection{2.4 Underbrace}
$\underbrace{O^*(m; A)}_{\text{ground truth, never asserted from discourse}} \neq \underbrace{\hat p_{A,\alpha,t}(m)}_{\text{inferred belief}}$


\subsection{2.5 Labeled arrows (world transition vs learner update)}
$m_t \xrightarrow{a_t} m_{t+1}$ (occurrence lineage) is not $\theta_t \xrightarrow{U_A(e_t)} \theta_{t+1}$ (learner update).


\subsection{2.6 Table containing inline math}

\begin{center}
\begin{tabular}{@{}p{0.313\linewidth}p{0.313\linewidth}p{0.313\linewidth}@{}}
\toprule
\textbf{Object} & \textbf{Symbol} & \textbf{Non-claim} \\
\midrule
observation map & $\Omega_{A,\alpha,t}$ & not the occurrence \\
inferred profile & $\hat p_{A,\alpha,t}(m)$ & not $O^*(m; A)$ \\
metaorthemma & $\bar\mu_{e,j}$ & not the metaortheme type $\mu$ \\
\bottomrule
\end{tabular}
\end{center}
All notation meanings are those of \texttt{docs/notation-registry.yaml}; nothing here introduces a new symbol or claim.


\twocolumn
\bibliographystyle{plainnat}
\bibliography{../../../references/orthemology}
\end{document}
