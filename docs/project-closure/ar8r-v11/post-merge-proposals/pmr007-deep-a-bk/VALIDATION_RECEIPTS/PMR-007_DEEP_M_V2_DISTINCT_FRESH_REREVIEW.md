# PMR-007 Deep Round M V2 distinct fresh rereview

```text
identity: PMR-007-ETRP-1
version: V2
same-model independence: distinct recursive simulator, hidden-architecture control, and profile-expansion enumeration
external human independence: NOT CLAIMED
disposition: PASS_WITH_NONBLOCKING_INTENTIONALITY_AND_WORLD_SCOPE_NOTES
```

## Hash and model result

```text
frozen hash rows:                    6
hash mismatches:                     0
random three-state transducers:      5,000
random traces up to length 20:       100,000
trace-copy mismatches:               0
```

The all-length theorem remains deductive: identical `delta` and `alpha` tables yield identical traces by induction. The random traces are regression evidence, not the proof.

## Boundary versus hidden architecture

The rereview constructed a one-state constant-output machine and a two-hidden-state toggling machine. They agree on every boundary output word tested while differing internally:

```text
boundary words checked:              1,023
boundary output differences:         0
hidden-state differences:            682
```

Thus boundary traces do not recover internal causal decomposition or bearer architecture. Round M correctly excludes internal-intervention equivalence.

## Profile-fibre result

```text
neutral profiles checked:            256
semantic/normative/personal expansions: 4,096
fibres with personality variation:   256 / 256
```

Within the declared model class, `PERS`, `MENT`, and intentional-uptake coordinates are not functions of the boundary profile. This establishes logical/profile underdetermination only.

## Central bridge result

```text
token responsiveness -> semantic reasonhood:
NOT ENTAILED

token responsiveness + target stability -> objective fittingness:
NOT ENTAILED

common structural carrier -> intentional subject:
NOT ENTAILED

boundary profile -> personality:
NOT ENTAILED IN THE DECLARED TWO-EXPANSION CLASS

intentional ownership:
REMAINS A POSSIBLE PARITY BREAKER ONLY IF INDEPENDENTLY WARRANTED
```

## Nonblocking notes

1. A richer model class with independent intrinsic-intentionality or phenomenological axioms can exclude the impersonal expansion; that would be new evidence, not a consequence of the boundary profile.
2. No metaphysical possibility, equal likelihood, equal prior, or physical implementation result is inferred.
3. The theorem is a scoped application of profile-factorization, finite transducer semantics, and language-reduct invariance. General mathematical novelty is zero.

## Disposition

```text
fresh rereview:
PASS_WITH_NONBLOCKING_INTENTIONALITY_AND_WORLD_SCOPE_NOTES

status:
ADMITTED_POST_MERGE_SCOPED_CENTRAL_BRIDGE_NEGATIVE_RESULT

integrated neutral champion:
NONE

strongest surviving rival:
IMPERSONAL_INTEGRATED_POWERS_CONTROLLER_WITH_THE_SAME_BOUNDARY_REASON_TOKEN_PROFILE
```
