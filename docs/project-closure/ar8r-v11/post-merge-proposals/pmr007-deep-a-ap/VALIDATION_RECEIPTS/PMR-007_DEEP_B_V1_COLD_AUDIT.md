# PMR-007 Deep Round B V1 — cold audit

```text
audit epoch: B
candidate: PMR-007-DURP-1
frozen packet: PMR-007_DEEP_B_V1_FROZEN_HASHES.sha256
disposition: PASS_WITH_NONBLOCKING_REALIZATION_AND_META_ABDUCTIVE_NOTES
blocking findings: 0
```

## Custody

All five frozen packet owners match their SHA-256 entries.

## Positive construction

The construction is genuinely non-boxed under Candidate G's declared criterion:
`Theta` has four profiles, every one of the five marginals is binary, and the
joint image contains only four of the 32 marginally possible profiles.  The laws
`M=S=N` and `R=A∧N` create shared derivation and intervention dependence.

## Theorem

`DURP-1` is correct by explanatory-structure isomorphism.  It is conditional on
preserving the declared primitive profiles, output map, interventions, and cost.
An idle realization label cannot be recovered from data that omit it.

## Nonblocking findings

### DB-N01 — functional parity is not full metaphysical equivalence

The theorem blocks inference from the retained explanatory structure.  It does
not prove that personal, impersonal, and distributed realizers are identical in
all respects or that richer evidence could not discriminate them.

### DB-N02 — distributed cost parity is conditional

An actual distributed system may require extra coupling, communication,
coordination, synchronization, or ownership machinery.  `H-DIST` is a matched
rival only where those costs are included in `C` and remain equal.  The theorem
already states this guard.

### DB-N03 — the truth/norm primitive is not grounded

The positive architecture identifies `M`, `S`, and `N` through the independently
supplied `t`.  This demonstrates cross-profile restriction; it does not explain
truth, normativity, proper function, or teleology.

### DB-N04 — derivational unity is not automatically truth-conducive

A false or gerrymandered unifier may compress the same data.  Direction,
independent motivation, fit, and rival comparison remain open meta-abductive
premises.

### DB-N05 — theorem-family ceiling

The result is a strict application and realization-parity corollary of Candidate
G's exact theorem and standard structure-isomorphism reasoning.  General
mathematical novelty is zero.

## Rereview requirement

A distinct implementation must reconstruct the dependency graph and canonical
structural signature from the output equations, verify the 28 exclusions, and
confirm that changing only the realization label leaves every retained
prediction and intervention signature invariant.
